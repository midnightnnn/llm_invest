Lists rule-based research/news documents for broad market themes and single-name analysis. The list response is metadata only and includes source_doc_id plus source_url. To inspect evidence, call the same tool again with source_doc_ids; it live-fetches the linked source text and returns a chunk with content_text, content_hash, and next_offset when more text is available.
